defaults: 
  - _self_
  - model: finetune 
  - dataset:  sciplex  # Fork patch (williamrobotma): `sciplex` exists in this checkout; the upstream default points to a missing dataset config here.
  - training: default 
  - wandb: default
  - hydra: default

profiling.run_profiler: False
profiling.outdir: "./"
